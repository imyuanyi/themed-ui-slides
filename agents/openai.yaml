interface:
  display_name: "Themed UI Slides"
  short_description: "Art-direct micro UI assets for polished HTML slides"
  default_prompt: "Use $themed-ui-slides with $frontend-slides to lock the visual theme, build live HTML typography, and, when I authorize parallel delegation, assign a UI-art agent to generate extractable style-matched micro UI assets with an available image-generation capability."
